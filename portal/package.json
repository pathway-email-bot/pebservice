{
  "name": "student-portal",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "copy-scenarios": "node scripts/copy-scenarios.js",
    "predev": "npm run copy-scenarios",
    "prebuild": "npm run copy-scenarios",
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^7.2.4"
  },
  "dependencies": {
    "firebase": "^12.8.0"
  }
}
